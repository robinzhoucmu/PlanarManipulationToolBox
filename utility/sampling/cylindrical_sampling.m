% Given the radius and height of the cylinder, sample uniformly in the
% cylindrical space. In each z-height plane, polar coordinate based
% sampling (sun flower sampling) is used.  
function [x,y,theta] = cylindrical_sampling(r, h)
end